# Frontend

React + Vite frontend for GenUI Chat. Streams responses from the FastAPI backend and renders generative UI components (pie charts, flight cards) inline in the chat.

## Setup

```bash
npm install
```

## Running

```bash
npm run dev
```

Expects the backend running on port 8003.
